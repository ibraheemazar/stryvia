# STRYVIA local Postgres credentials.
#
# Copy this file to `.env` and fill in real values. `.env` is gitignored.
# Generate POSTGRES_PASSWORD with: openssl rand -base64 24

POSTGRES_USER=stryvia
POSTGRES_PASSWORD=replace-with-strong-random-value
POSTGRES_DB=stryvia_dev
