# STRYVIA Producer — local environment.
# Copy this file to `.env.local` (which is gitignored) and adjust values as needed.

# URL of the Python valuation engine (FastAPI service).
NEXT_PUBLIC_ENGINE_URL=http://localhost:8000

# Public-facing application name (rendered in the browser tab and metadata).
NEXT_PUBLIC_APP_NAME=STRYVIA
